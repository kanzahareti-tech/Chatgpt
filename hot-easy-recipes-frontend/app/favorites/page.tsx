export default function FavoritesPage() {
  return (
    <section style={{ padding: '2rem' }}>
      <h2>Favorites</h2>
      <p>Saved recipes (client-side storage) placeholder.</p>
    </section>
  );
}
